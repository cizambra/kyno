"""Adapters that wire the Kyno control plane into agent orchestrators.

The framework-agnostic core lives in ``kyno.adapters.core``; per-framework
shims live in ``kyno.adapters.crewai`` and ``kyno.adapters.langgraph`` and
are installed with the ``kyno[crewai]`` / ``kyno[langgraph]`` extras.
"""
